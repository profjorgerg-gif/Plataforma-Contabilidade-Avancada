name: Atualizar backup ao sair
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/update-logout-backup.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Aplicar atualização
        run: python .github/scripts/add_logout_backup.py
      - name: Validar JavaScript
        run: node --check public/app.js
      - name: Commit funcional
        run: |
          git config user.name 'Projetos Pedagogicos'
          git config user.email 'profjorge.rg@gmail.com'
          git add public/app.js public/index.html README.md
          git diff --cached --quiet && exit 0
          git commit -m 'Adiciona backup opcional antes de sair'
          git push origin main
